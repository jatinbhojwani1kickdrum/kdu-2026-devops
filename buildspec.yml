version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21
    commands:
      - chmod +x mvnw
      - ./mvnw -v
  build:
    commands:
      - echo Build started on `date`
      - ./mvnw clean package

artifacts:
  files:
    - target/*.war
  discard-paths: yes